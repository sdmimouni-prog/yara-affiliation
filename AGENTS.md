# Static client demo
Standalone React/Vite prototype. Keep this version independent of PHP, backend APIs, databases and production secrets.
Use fictitious data only. Keep the demonstration notice and direct demo access visible.
Desktop login should fit within one screen without scrolling.

Static desktop dashboard uses 80% visual scale for a compact client preview. Keep this adjustment isolated from the PHP-backed version.
